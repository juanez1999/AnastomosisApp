import React from 'react';

export const EmptyClasses = (params) => {

    return(
        <div>
            <h3>No tienes clases creadas</h3>
            <p>Presiona  el boton más (+) y 
            agrega tu primera clase.</p>
        </div>
    );
}
